import React from 'react';
import CreateBook from '../CreateBook';

const Add = () => (
  <div className="form">
    <h3>Add a new Book to the Catalog</h3>
    <CreateBook/>
  </div>
);

module.exports = Add;